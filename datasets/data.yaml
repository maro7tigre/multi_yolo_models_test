# Root directory for the dataset (relative or absolute)
path: .

# Relative paths to image folders within the dataset directory
train: images/train
val: images/val
# Optionally include test images for evaluation
test: # optional

# Number of object classes
nc: 5

# Class names in the order of their indices (starting with 0)
names: [cardboard, glass, metal, paper, plastic]